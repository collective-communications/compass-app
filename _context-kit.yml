# Culture Compass — Project Manifest
# Auto-generated by context-kit-yaml skill
# Format: v14 | Generated: 2026-03-16

meta:
  kit: compass-app
  fmt: 14
  type: fullstack-app
  desc: Culture assessment platform — survey administration, compass visualization, branded report generation
  ver: "0.1.0"
  ts: "2026-03-16T00:00:00Z"
  status: development
  entry: apps/web/src/main.tsx
  stack: "TypeScript + React 19 + Supabase + Vite + TanStack Router"
  cmds: [dev, build, test, "test:e2e", "db:seed", lint, typecheck, storybook]
  langs: {ts: 0.88, json: 0.05, sql: 0.05, md: 0.01, yaml: 0.01}

struct:
  _: {n: 532, tests: 57, t: {ts: 460, json: 32, sql: 27, md: 6, yaml: 4, css: 1}}
  apps/web/src:
    components: {n: 52, d: "app, brand, help, navigation, shells, survey, ui"}
    features: {n: 311, tests: 23, d: "admin(106), results(118), survey(47), reports(26), auth(20), dashboard(17)"}
    routes: {n: 1, d: "TanStack Router route tree — factory-based"}
    lib: {n: 3, d: "supabase client, env, utils"}
    hooks: {n: 1}
    stores: {n: 3}
    shells: {n: 3, d: "survey shell layout"}
  apps/storybook: {n: 8, d: "Storybook visual testing, templates, test-utils"}
  packages:
    compass: {n: 21, d: "SVG compass visualization"}
    scoring: {n: 23, d: "Scoring pipeline — dimensions, archetypes, risk flags, segments"}
    tokens: {n: 2, d: "Brand design tokens"}
    types: {n: 7, d: "Shared TypeScript types"}
    ui: {n: 1, d: "Shared UI components"}
    utils: {n: 2, d: "Shared utilities"}
  e2e: {tests: 15, d: "Playwright E2E — admin, dashboard, results, survey, help, reports"}
  supabase: {migrations: 26, functions: 6}
  tooling: {n: 6, d: "eslint, tailwind preset, tsconfig base"}

deps:
  prod: [react@19, "@supabase/supabase-js@2", "@tanstack/react-router@1", "@tanstack/react-query@5", zustand@5, lucide-react, "@dnd-kit/{core,sortable}"]
  dev: [vite@6, tailwindcss@4, typescript@5, happy-dom, playwright, "@testing-library/{react,dom,jest-dom}"]
  workspace: ["@compass/{compass,scoring,tokens,types,ui,utils}"]

nav:
  start: {frontend: apps/web/src/main.tsx, routing: apps/web/src/routes/__root.tsx, types: packages/types/src/index.ts, scoring: packages/scoring/src/index.ts}
  views: {"/": landing, "/auth/login": sign-in, "/s/$token": survey-engine, "/admin/*": admin, "/dashboard/*": client-dashboard, "/results/*": results-viz, "/reports/*": reports}
  reading_order: [packages/types → main.tsx → __root.tsx → survey feature → scoring pipeline → supabase migrations]

arch:
  stack: "TypeScript + React 19 + Supabase + Vite + Tailwind v4"
  patterns: [monorepo-workspaces, feature-based-arch, IoC-adapters, zustand-stores, structural-anonymity]
  services: {supabase: "BaaS (Auth+DB+Edge Functions)", web: "SPA on :5173"}
  edge_functions: [embed-responses, generate-report, score-survey, send-email, send-invitations, send-reminders]

config:
  env: [VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY]
  ports: &ports {web: 5173, supabase-api: 54321, supabase-db: 54322}
  db: {type: PostgreSQL, provider: Supabase}

testing:
  frameworks: [playwright, bun, happy-dom, "@testing-library/react"]
  files: {unit: 57, e2e: 15}
  gaps: ["component-level tests sparse", "reports feature under-tested"]

issues:
  total: 1
  byTag: {TODO: 1}

ops:
  ports: *ports
  scripts:
    dev: "bun run --filter @compass/web dev"
    build: "bun run --filter @compass/web build"
    test: "bun test apps packages"
    "test:e2e": "bun run --filter @compass/e2e test"
    lint: "eslint ."
    typecheck: "tsc -b"
    "db:push": "supabase db push"
    "db:seed": "bun run scripts/seed-dev.ts"
    "db:clean": "bun run scripts/seed-dev.ts --clean"
    storybook: "bun run --filter @compass/storybook storybook"

semantic:
  ~patterns: "Feature-based monorepo with IoC adapters, structural anonymity, 3-shell layout"
  ~conventions: "ES Modules, named exports, strict TypeScript, Tailwind v4, CSS custom properties"
  ~learning_path: "types → main.tsx → __root.tsx → survey feature → scoring pipeline → migrations"
  ~focus: "Culture assessment SaaS — anonymous surveys, compass visualization, branded reports"
