# @compass/ui

Shared UI component library built on shadcn/ui for the Culture Compass platform.

## Public API

Components are re-exported as they are added from shadcn/ui.

## Key Dependencies

- `react` — Component rendering
- `tailwindcss` — Styling
- `shadcn/ui` — Base component primitives
